# 首轮跑通断点报告（非战斗/非美术）

日期：2026-03-28  
范围：Scene1 主流程首轮接入验证（静态链路 + 编译 + 节点状态）。

## 1. 本轮已接入能力

1. 流程状态控制器接入
- [Scripts/Map/Flow/MapFlowController.cs](Scripts/Map/Flow/MapFlowController.cs)
- [Scene/Scene1.tscn](Scene/Scene1.tscn)

2. 关键交互节点门控（通过交互后写入 flow 状态）
- NPC：tutorial_interact -> hub_prepare
- 宝箱：hub_prepare -> hub_gate_maze
- 敌人：maze_fight_1 -> maze_event_1

相关场景：
- [Scene/Npc.tscn](Scene/Npc.tscn)
- [Scene/Chest.tscn](Scene/Chest.tscn)
- [Scene/Enemy.tscn](Scene/Enemy.tscn)

3. 最小存档点（空港入口）
- 脚本： [Scripts/Map/Flow/HubEntrySavePoint.cs](Scripts/Map/Flow/HubEntrySavePoint.cs)
- 场景挂载： [Scene/Scene1.tscn](Scene/Scene1.tscn)
- 存档文件：user://saves/min_checkpoint.cfg

4. 编译状态
- 当前 `get_errors` 结果：无错误。
- 仍有历史 nullable 警告（CS8632），不影响本轮流程功能接入。

## 2. 首轮跑通结果

结论：主流程“节点状态推进链”已可用，但“完整竞赛切片”仍未闭环。

已可验证链路：
- Scene1 内可通过交互推进关键 flow 键。
- 可在空港入口存档点记录最小检查点。

未闭环部分（断点）：
1. 教程完整链未落地
- 目前仅有 `intro_wakeup` 与 `tutorial_interact` 相关状态接入。
- `tutorial_move`、`tutorial_encounter` 仍缺事件驱动。

2. 空港 -> 迷宫多节点结构未完成
- 目前是单场景内最小节点映射，不是完整多段迷宫切片。
- 缺少 3-5 普通遭遇 + 1 精英 + 事件组的结构化节点网络。

3. Boss 前置与收束未接入
- 目前尚无 Boss 前置门与收束节点触发脚本。

4. 存档点仅实现“保存”，尚未接“读取恢复”流程
- 已能落盘最小状态。
- 还未增加启动/读取时的回档入口与应用逻辑。

## 3. 下一轮可执行修复位点

1. 教程节点补齐
- 新增教程事件触发器，补完 `tutorial_move`、`tutorial_encounter`。

2. 迷宫切片节点化
- 按配置表建立 encounter/event/gate 节点组，至少先做 1 普通 + 1 精英 + 1 前置门。

3. Boss 前置门
- 新增 `boss_gate` 节点门控并绑定前置条件。

4. 读取恢复
- 增加最小读档方法，将 `flow.active_node` 与已完成节点回填到 `GameSession`。

## 4. 判定

- P0 子任务“关键节点接入 + 最小存档点”已完成首版实现。  
- P0 子任务“首轮跑通并输出断点报告”已完成（本文件）。  
- 进入下一轮：补齐教程事件与迷宫/Boss 前置节点。
