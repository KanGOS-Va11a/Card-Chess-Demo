# 地图层二次合并执行记录 - 2026-04-12

## 本次确认的基线

- 地图场景以 donor 为准：
  - `Scene01`
  - `Scene03`
  - `Scene04`
  - `Scene05`
  - `Scene06`
  - `Scene07`
  - `Scene08`
- 卡牌链路、背包、C 键菜单系统以当前主工程为准。
- 战斗系统完全以当前主工程为准。
- donor 的“橱柜储物面板”不再使用，统一改走当前主工程的地图交互链路。

## 本次已执行内容

### 1. donor 地图场景覆盖到主工程

已用 donor 版本覆盖：

- `Scene/Maps/Scene01.tscn`
- `Scene/Maps/Scene03.tscn`
- `Scene/Maps/Scene04.tscn`
- `Scene/Maps/Scene05.tscn`
- `Scene/Maps/Scene06.tscn`
- `Scene/Maps/Scene07.tscn`
- `Scene/Maps/Scene08.tscn`

说明：

- 这些场景仍然挂接当前主工程的 `MapSceneController`、`MapResolutionController`、`SystemFeatureOverlay`、`MainPlayer`、`Enemy`、`Door` 等节点/脚本路径。
- 因此地图内容来自 donor，但系统接线仍然走主工程逻辑。

### 2. Cabinet 交互改回主工程统一链路

已修改：

- `Scripts/Map/Interaction/Cabinet.cs`

改动结果：

- 不再打开 donor 的 `LootContainerOverlay` 储物柜面板。
- 改为一次性交互：
  - 显示场景内文字提示
  - 将配置物品直接加入当前背包
  - 标记为已搜刮
  - 后续再次交互显示“已搜空”
- 保留 session key / runtime snapshot 逻辑，避免跨场景或返回后重复拿取。

## 当前状态

- `dotnet build` 已通过。
- donor 地图主线已经切到主工程工作区。

## 下一步建议

- 检查 donor 这几张地图中的门传送目标是否符合当前流程设计。
- 检查 donor 场景内敌人的 `EncounterId` 是否已经按需要区分。
- 再处理剧情触发、教程脚本、地图流程控制的冲突项。
